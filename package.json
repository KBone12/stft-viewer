{
  "name": "stft-viewer",
  "version": "0.0.0",
  "description": "View spectra!",
  "author": "kbone",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rimraf dist pkg && webpack --mode=production",
    "start": "rimraf dist pkg && webpack-dev-server -d --mode=development",
    "test": "cargo test && wasm-pack test --headless"
  },
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1.3.1",
    "copy-webpack-plugin": "^6.3.2",
    "rimraf": "^3.0.2",
    "webpack": "^4.44.2",
    "webpack-cli": "^3.3.12",
    "webpack-dev-server": "^3.11.0",
    "worker-loader": "^3.0.5"
  }
}
